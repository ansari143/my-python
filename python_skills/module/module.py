'''
two type of module

built-in module
__future__, __main__, abc, aifc, argparse, array, ast, asynchat, asyncio, asyncore, atexit, audioop, base64, bdb, binascii, bisect, builtins, bz2, calendar, cgi, cgitb, 
chunk, cmath, cmd, code, codecs, codeop, collections, collections.abc, colorsys, compileall, concurrent, concurrent.futures, configparser, contextli
https://docs.python.org/3/py-modindex.html

external module 
'''

import math
import os
import mymodule
print(math.sqrt(16))
print(os.curdir)
mymodule.greeting("ansari")